@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/SqlBuilderTargets.cmake")

# 可选：报告项目版本
set(SqlBuilder_VERSION @PROJECT_VERSION@)

# 检查必需组件（本库目前没有组件划分）
check_required_components(SqlBuilder)

# 可选：打印找到信息
message(STATUS "SqlBuilder found (version: ${SqlBuilder_VERSION})")
